name: KARTA Unsigned Android Release

on:
  push:
    branches: ['release/**']
    paths:
      - 'apps/mobile/**'
      - '.github/workflows/unsigned-android-release.yml'

permissions:
  contents: read

jobs:
  unsigned-android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile
    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Android platform scaffold
        run: flutter create --platforms=android --android-language=kotlin --project-name=karta_wallet --org=com.karta.identity --no-pub .

      - name: Install native bridge and KARTA branding
        run: python3 scripts/install_android.py

      - name: Verify KARTA launcher branding
        shell: bash
        run: |
          set -euo pipefail
          grep -n 'android:label="KARTA"' android/app/src/main/AndroidManifest.xml
          grep -n '@drawable/karta_launcher' android/app/src/main/AndroidManifest.xml
          test -f android/app/src/main/res/drawable/karta_launcher.xml
          test -f android/app/src/main/res/drawable/launch_background.xml

      - name: Disable debug signing for release outputs
        shell: bash
        run: |
          set -euo pipefail
          FILE=android/app/build.gradle.kts
          grep -n 'signingConfig' "$FILE" || true
          python3 - <<'PY'
          from pathlib import Path
          p = Path('android/app/build.gradle.kts')
          s = p.read_text()
          old = 'signingConfig = signingConfigs.getByName("debug")'
          if old not in s:
              raise SystemExit('Expected Flutter debug release signing line was not found')
          p.write_text(s.replace(old, 'signingConfig = null', 1))
          PY
          grep -n 'signingConfig' "$FILE" || true

      - name: Resolve dependencies
        run: flutter pub get

      - name: Analyze Dart code
        run: flutter analyze

      - name: Run Flutter tests
        run: flutter test

      - name: Build unsigned release APK
        run: flutter build apk --release

      - name: Build unsigned release AAB
        run: flutter build appbundle --release

      - name: Record unsigned hashes
        shell: bash
        run: |
          sha256sum build/app/outputs/flutter-apk/app-release.apk | tee build/app-release-unsigned-apk.sha256
          sha256sum build/app/outputs/bundle/release/app-release.aab | tee build/app-release-unsigned-aab.sha256

      - name: Upload unsigned release APK
        uses: actions/upload-artifact@v4
        with:
          name: karta-0.9-rc1-build13-unsigned-apk
          path: |
            apps/mobile/build/app/outputs/flutter-apk/app-release.apk
            apps/mobile/build/app-release-unsigned-apk.sha256
          if-no-files-found: error
          retention-days: 3

      - name: Upload unsigned release AAB
        uses: actions/upload-artifact@v4
        with:
          name: karta-0.9-rc1-build13-unsigned-aab
          path: |
            apps/mobile/build/app/outputs/bundle/release/app-release.aab
            apps/mobile/build/app-release-unsigned-aab.sha256
          if-no-files-found: error
          retention-days: 3
