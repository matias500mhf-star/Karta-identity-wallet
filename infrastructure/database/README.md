# KARTA Database

PostgreSQL is the planned primary relational database for KARTA.

The database layer will contain migrations for users, identity profiles, devices, documents, document versions, sharing permissions, verification requests, audit logs, and notifications.

No production credentials belong in this repository.
