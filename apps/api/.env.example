NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://karta:change-me@localhost:5432/karta
# Generate with: openssl rand -hex 32
JWT_ACCESS_SECRET=
# Registration stays closed when this is unset. Share only with invited testers.
BETA_INVITE_CODE=
# The old server-decrypted document routes are disabled in the backup beta.
ENABLE_LEGACY_DOCUMENTS=false
